{
  "name": "smartodo-ce",
  "version": "1.0.0",
  "description": "SmartTodo Community Edition - A monorepo for todo management application",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "dev": "lerna run dev --parallel"
  },
  "keywords": ["monorepo", "lerna", "todo", "typescript"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
